import Wizard from './Wizard.js';

export default class Andrew extends Wizard {
    constructor(scene, x, y) {
        super(scene, x, y, 'Andrew', 0x0000FF, 0); // Example: blue tint, frame 0
    }
}
